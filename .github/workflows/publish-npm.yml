name: Publish → npm

on:
  workflow_dispatch:
  workflow_call:

jobs:
  publish:
    name: npm 배포
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Node.js 설치
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org/

      - name: Rust Node.js 바인딩 빌드
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --release --locked -p dbx-node

      - name: npm 의존성 설치
        run: |
          cd packages\nodejs
          npm install

      - name: npm 패키지 생성
        run: |
          cd packages\nodejs
          npm pack

      - name: npm 배포
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          cd packages\nodejs
          npm publish --access public --ignore-scripts

      - name: 에셋 업로드
        uses: actions/upload-artifact@v4
        with:
          name: npm-package
          path: packages\nodejs\*.tgz
