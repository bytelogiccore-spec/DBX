name: 릴리스 및 배포

on:
  workflow_dispatch:
    inputs:
      version:
        description: '릴리스 버전 (예: v0.1.0)'
        required: true
        type: string
      publish_crates:
        description: 'crates.io 배포'
        type: boolean
        default: true
      publish_nuget:
        description: 'NuGet 배포'
        type: boolean
        default: true
      publish_pypi:
        description: 'PyPI 배포'
        type: boolean
        default: true
      publish_npm:
        description: 'npm 배포'
        type: boolean
        default: true

jobs:
  # ──────────────────────────────────────────────
  # Phase 1: Rust 코어 검증
  # ──────────────────────────────────────────────
  build-and-test:
    name: Rust 코어 빌드 및 테스트
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Rust 설치
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Rust 캐시
        uses: Swatinem/rust-cache@v2

      - name: 포맷 검사
        run: cargo fmt --all -- --check

      - name: Lint 검사 (dbx-core)
        run: cargo clippy -p dbx-core -- -D warnings

      - name: 빌드 (Release)
        run: cargo build --release --locked -p dbx-core -p dbx-ffi

      - name: 테스트
        run: cargo test -p dbx-core --locked

  # ──────────────────────────────────────────────
  # Phase 2: 각 패키지 매니저 배포 (독립 실행)
  # ──────────────────────────────────────────────

  publish-crates:
    needs: build-and-test
    if: ${{ inputs.publish_crates }}
    uses: ./.github/workflows/publish-crates.yml
    secrets: inherit

  publish-nuget:
    needs: build-and-test
    if: ${{ inputs.publish_nuget }}
    uses: ./.github/workflows/publish-nuget.yml
    secrets: inherit

  publish-pypi:
    needs: build-and-test
    if: ${{ inputs.publish_pypi }}
    uses: ./.github/workflows/publish-pypi.yml
    secrets: inherit

  publish-npm:
    needs: build-and-test
    if: ${{ inputs.publish_npm }}
    uses: ./.github/workflows/publish-npm.yml
    secrets: inherit

  # ──────────────────────────────────────────────
  # Phase 3: C/C++ 네이티브 빌드
  # ──────────────────────────────────────────────
  build-native:
    name: C/C++ 네이티브 빌드
    runs-on: windows-latest
    needs: build-and-test
    steps:
      - uses: actions/checkout@v4

      - name: Rust 설치
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Release 빌드
        run: cargo build --release --locked -p dbx-ffi

      - name: C/C++ 패키지 준비
        run: |
          New-Item -Path dbx-native -ItemType Directory -Force
          Copy-Item target/release/dbx_ffi.dll dbx-native/
          Copy-Item target/release/dbx_ffi.dll.lib dbx-native/ -ErrorAction SilentlyContinue
          Copy-Item target/release/dbx_ffi.lib dbx-native/ -ErrorAction SilentlyContinue
          Copy-Item lang/c/include/*.h dbx-native/ -ErrorAction SilentlyContinue
          Copy-Item lang/cpp/include/*.h dbx-native/ -ErrorAction SilentlyContinue
          Copy-Item lang/cpp/include/*.hpp dbx-native/ -ErrorAction SilentlyContinue
          Compress-Archive -Path dbx-native/* -DestinationPath dbx-native-windows-x64.zip

      - name: 네이티브 에셋 업로드
        uses: actions/upload-artifact@v4
        with:
          name: native-package
          path: dbx-native-windows-x64.zip

  # ──────────────────────────────────────────────
  # Phase 4: GitHub Release (모든 에셋 첨부)
  # ──────────────────────────────────────────────
  github-release:
    name: GitHub 릴리스 생성
    runs-on: ubuntu-latest
    if: ${{ always() }}
    needs: [publish-crates, publish-nuget, publish-pypi, publish-npm, build-native]
    steps:
      - uses: actions/checkout@v4

      - name: 모든 에셋 다운로드
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: 에셋 목록 확인
        run: ls -laR release-assets/ || echo "No artifacts found"

      - name: 릴리스 생성
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}
          name: DBX ${{ inputs.version }}
          body: |
            ## 📦 Downloads

            | Package | Install |
            |---------|---------|
            | **Rust** | `cargo add dbx-core` |
            | **C#/.NET** | `dotnet add package DBX.Dotnet` |
            | **Python** | `pip install dbx-py` |
            | **Node.js** | `npm install dbx-native` |
            | **C/C++** | `dbx-native-windows-x64.zip` (아래 첨부) |

            ---
          files: release-assets/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
