[package]
name = "dbx-node"
version.workspace = true
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
dbx-core = { path = "../dbx-core", version = "0.2.1-beta" }
napi = { version = "3", features = ["async"] }
napi-derive = "3"

[build-dependencies]
napi-build = "2"
